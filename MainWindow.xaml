<Window x:Class="PrintMaster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PrintMaster – Workflows"
        Height="480" Width="900"
        MinHeight="350" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Überschrift -->
        <TextBlock Grid.Row="0" Text="PrintMaster" FontSize="22" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,12"/>

        <!-- DataGrid: aktive Workflows -->
        <DataGrid Grid.Row="1" x:Name="WorkflowsGrid"
                  ItemsSource="{Binding Workflows}"
                  SelectedItem="{Binding SelectedWorkflow, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeight="32"
                  Margin="0,0,0,16"
                  BorderThickness="1" BorderBrush="#DDD"
                  Background="White"
                  MouseDoubleClick="WorkflowsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Aktiv" Width="50" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsEnabled, Mode=OneWay}" HorizontalAlignment="Center"
                                      Click="WorkflowActive_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="140" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ordner" Binding="{Binding WatchPath}" Width="*" MinWidth="180" IsReadOnly="True"/>
                <DataGridTextColumn Header="Muster" Binding="{Binding FilePattern}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Drucker" Binding="{Binding PrinterName}" Width="160" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons + Status: Buttons haben Vorrang, Status nutzt Rest und kürzt mit ... -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="0"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BtnNewWorkflow" Content="Neuer Workflow" Click="BtnNewWorkflow_Click" Margin="0,0,8,0"/>
                <Button x:Name="BtnEditWorkflow" Content="Bearbeiten" Click="BtnEditWorkflow_Click" Margin="0,0,8,0"/>
                <Button Content="Duplizieren" Command="{Binding DuplicateWorkflowCommand}" Margin="0,0,8,0"/>
                <Button Content="Löschen" Command="{Binding DeleteWorkflowCommand}" Margin="0,0,8,0"/>
                <Button x:Name="BtnLog" Content="Log" Click="BtnLog_Click" Margin="0,0,8,0"/>
                <Button Content="Drucker aktualisieren" Command="{Binding RefreshPrintersCommand}" Margin="0,0,8,0"/>
                <Button x:Name="BtnSettings" Content="Einstellungen" Click="BtnSettings_Click"/>
            </StackPanel>
            <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0" TextTrimming="CharacterEllipsis"
                       Text="{Binding StatusMessage}" Foreground="#666" FontStyle="Italic"/>
        </Grid>
    </Grid>
</Window>
